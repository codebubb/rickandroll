name: Build and Deploy
on:
  push:
    branches: [main]
jobs:
  build:
    runs-on: ubuntu-latest
    environment: Production
    steps:
      - name: Build
        uses: actions/setup-node@v1
        with:
          node-version: '16.x'
        run: npm ci